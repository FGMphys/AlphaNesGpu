# Deprecated tree

Use the unified **`STAF/`** package:

```bash
cd ../STAF
bash install_path.sh float
python staf_train.py input_staf.yaml   # YAML: precision: float
```

This directory only keeps thin redirects for old scripts.
